# KitchenSync Requirements

# MIDI Support
python-rtmidi>=1.4.0
mido>=1.2.10

# VLC Python bindings for video control
python-vlc>=3.0.0

# Pygame for debug overlay graphics (optional)
pygame>=2.0.0

# DBus support (optional, for legacy compatibility)
dbus-python>=1.2.0

# Standard library modules used:
# - socket, json, time, subprocess, threading
# - configparser, pathlib, os, sys, statistics, collections

# System Dependencies (install via apt):
# sudo apt install vlc libvlc-dev python3-vlc python3-pip python3-venv python3-dev libasound2-dev

# Installation:
# python3 -m venv kitchensync-env
# source kitchensync-env/bin/activate
# pip install -r requirements.txt
#   - mplayer
