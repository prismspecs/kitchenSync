# kSync pip requirements — installed by setup.sh into ./.venv
# (GStreamer/gi bindings come from apt, NOT pip; the venv uses --system-site-packages.)

# MIDI / serial (dormant feature — see docs/MIDI_CONTROL.md)
mido>=1.2.10
pyserial>=3.5

# OSC (scaffold — see .agents/skills/ksync-research-frontier F5)
python-osc>=1.8.3

# Optional, only for direct USB-MIDI hardware output (needs apt libasound2-dev to build):
# python-rtmidi>=1.4.0

# Manual install (setup.sh does this for you):
#   python3 -m venv --system-site-packages .venv
#   .venv/bin/pip install -r requirements.txt
